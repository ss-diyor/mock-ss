diff --git a/auth.py b/auth.py
index de33061..bd9d281 100644
--- a/auth.py
+++ b/auth.py
@@ -421,14 +421,15 @@ async def register(data: RegisterIn):
                 group_row = await conn.fetchrow(
                     """
                     UPDATE groups
-                    SET teacher_invite_code = NULL
+                    SET teacher_invite_code = NULL, teacher_invite_expires_at = NULL
                     WHERE teacher_invite_code = $1 AND teacher_id IS NULL
+                      AND (teacher_invite_expires_at IS NULL OR teacher_invite_expires_at > NOW())
                     RETURNING id, name, center_id, is_active
                     """,
                     data.teacher_invite_code
                 )
                 if not group_row:
-                    raise HTTPException(status_code=400, detail="Taklif kodi noto'g'ri yoki allaqachon ishlatilgan")
+                    raise HTTPException(status_code=400, detail="Taklif kodi noto'g'ri, eskirgan yoki allaqachon ishlatilgan")
                 if not group_row["is_active"]:
                     raise HTTPException(status_code=400, detail="Bu guruh faol emas")
                 role_val = "teacher"
diff --git a/exports.py b/exports.py
new file mode 100644
index 0000000..1077277
--- /dev/null
+++ b/exports.py
@@ -0,0 +1,53 @@
+"""
+CSV/Excel export uchun umumiy yordamchi.
+Admin, head-teacher va teacher panellari natijalarni bir xil formatda export qiladi —
+shu mantiqni uch joyda takrorlamaslik uchun shu yerga chiqarildi.
+"""
+
+import csv
+import io
+
+try:
+    import openpyxl
+except ImportError:
+    openpyxl = None
+
+from fastapi import HTTPException
+from fastapi.responses import StreamingResponse
+
+
+def export_rows(data: list[dict], filename_base: str, format: str = "csv", sheet_title: str = "Sheet1"):
+    """`data` — list of flat dicts (bir xil kalitlar bilan). CSV yoki Excel qilib qaytaradi."""
+    if format == "csv":
+        output = io.StringIO()
+        if data:
+            writer = csv.DictWriter(output, fieldnames=data[0].keys())
+            writer.writeheader()
+            writer.writerows(data)
+        output.seek(0)
+        return StreamingResponse(
+            iter([b'\xef\xbb\xbf' + output.getvalue().encode('utf-8')]),
+            media_type="text/csv",
+            headers={"Content-Disposition": f"attachment; filename={filename_base}.csv"}
+        )
+
+    if format == "excel":
+        if not openpyxl:
+            raise HTTPException(status_code=500, detail="openpyxl o'rnatilmagan")
+        wb = openpyxl.Workbook()
+        ws = wb.active
+        ws.title = sheet_title
+        if data:
+            ws.append(list(data[0].keys()))
+            for d in data:
+                ws.append(list(d.values()))
+        output = io.BytesIO()
+        wb.save(output)
+        output.seek(0)
+        return StreamingResponse(
+            output,
+            media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
+            headers={"Content-Disposition": f"attachment; filename={filename_base}.xlsx"}
+        )
+
+    raise HTTPException(status_code=400, detail="Noto'g'ri format — 'csv' yoki 'excel' bo'lishi kerak")
diff --git a/grading.py b/grading.py
new file mode 100644
index 0000000..35c1b8e
--- /dev/null
+++ b/grading.py
@@ -0,0 +1,253 @@
+"""
+Writing baholash, PDF sertifikat va email yuborish uchun umumiy mantiq.
+Ilgari bu kod faqat main.py'da (admin uchun) edi. Endi teacher ham o'z guruhi
+doirasida Writing baholashi kerak bo'lgani uchun umumiy funksiyalarga chiqarildi —
+shunda ikkala joyda ham bir xil email/PDF/Telegram bildirishnoma logikasi ishlaydi.
+"""
+
+import base64
+import os
+from datetime import datetime
+
+import httpx
+from fastapi import HTTPException
+from fpdf import FPDF
+
+from db import get_pool
+from scoring import get_band_score
+
+RESEND_API_KEY = os.environ.get("RESEND_API_KEY")
+EMAIL_FROM = os.environ.get("EMAIL_FROM", "noreply@ielts.sultanov.space")
+
+
+async def send_email_with_attachment(
+    to_email: str, to_name: str, subject: str, html_body: str,
+    attachment_bytes: bytes, attachment_filename: str
+):
+    if not RESEND_API_KEY:
+        raise RuntimeError("RESEND_API_KEY sozlanmagan")
+
+    b64_content = base64.b64encode(attachment_bytes).decode('utf-8')
+
+    async with httpx.AsyncClient(timeout=15) as client:
+        response = await client.post(
+            "https://api.resend.com/emails",
+            headers={
+                "Authorization": f"Bearer {RESEND_API_KEY}",
+                "Content-Type": "application/json"
+            },
+            json={
+                "from": f"IELTS Mock <{EMAIL_FROM}>",
+                "to": [to_email],
+                "subject": subject,
+                "html": html_body,
+                "attachments": [
+                    {
+                        "filename": attachment_filename,
+                        "content": b64_content
+                    }
+                ]
+            }
+        )
+        if response.status_code >= 400:
+            raise RuntimeError(f"Resend xatosi: {response.text}")
+
+
+def build_result_email(name: str, section: str, score, total, band, feedback=None) -> str:
+    section_names = {"listening": "Listening", "reading": "Reading", "writing": "Writing"}
+    section_name = section_names.get(section, section)
+
+    if section == "writing":
+        body = f"""
+        <p><b>Band Score:</b> {band if band is not None else 'Hali baholanmagan'}</p>
+        {f'<p><b>Izoh:</b> {feedback}</p>' if feedback else ''}
+        """
+    else:
+        body = f"""
+        <p><b>Natija:</b> {score} / {total} to'g'ri</p>
+        <p><b>Band Score:</b> {band}</p>
+        """
+
+    return f"""
+    <div style="font-family:Arial,sans-serif; max-width:520px; margin:0 auto; padding:24px; border:1px solid #c9d8ff; border-radius:12px;">
+      <h2 style="color:#1a56e8;">IELTS Mock — {section_name} natijasi</h2>
+      <p>Assalomu alaykum, {name}!</p>
+      {body}
+      <p style="margin-top:20px; color:#4a5978; font-size:13px;">
+        Batafsil: <a href="https://ielts.sultanov.space" style="color:#1a56e8;">ielts.sultanov.space</a>
+      </p>
+      <p style="color:#4a5978; font-size:12px; margin-top:16px;"> © 2026-2027 Bo'stonliq tuman ixtisoslashtirilgan maktabi</p>
+    </div>
+    """
+
+
+def build_result_pdf(full_name: str, email: str, sections: list) -> bytes:
+    pdf = FPDF(orientation="P", unit="mm", format="A4")
+    pdf.add_page()
+    pdf.set_auto_page_break(auto=False)
+
+    # Header
+    pdf.set_fill_color(26, 86, 232)
+    pdf.rect(0, 0, 210, 30, style="F")
+    pdf.set_xy(0, 9)
+    pdf.set_text_color(255, 255, 255)
+    pdf.set_font("Helvetica", "B", 18)
+    pdf.cell(210, 8, "IELTS Mock Exam - Natija", align="C")
+    pdf.set_xy(0, 18)
+    pdf.set_font("Helvetica", "", 10)
+    pdf.cell(210, 6, "ielts.sultanov.space", align="C")
+
+    # Demo disclaimer banner
+    pdf.set_xy(10, 36)
+    pdf.set_fill_color(255, 251, 235)
+    pdf.set_draw_color(217, 119, 6)
+    pdf.rect(10, 36, 190, 12, style="DF")
+    pdf.set_xy(12, 39)
+    pdf.set_text_color(217, 119, 6)
+    pdf.set_font("Helvetica", "", 9)
+    pdf.multi_cell(186, 5,
+        "Diqqat: bu DEMO test rejimi natijasi. Savollar namunaviy, haqiqiy imtihon "
+        "formatidagi savollar hali qo'shilmagan. Rasmiy sertifikat emas.")
+
+    # Candidate info
+    y = 56
+    pdf.set_text_color(11, 23, 51)
+    pdf.set_xy(10, y)
+    pdf.set_font("Helvetica", "B", 12)
+    pdf.cell(95, 7, "Ism-familiya:")
+    pdf.set_font("Helvetica", "", 12)
+    pdf.cell(95, 7, full_name)
+    pdf.set_xy(10, y + 7)
+    pdf.set_font("Helvetica", "B", 12)
+    pdf.cell(95, 7, "Email:")
+    pdf.set_font("Helvetica", "", 12)
+    pdf.cell(95, 7, email)
+    pdf.set_xy(10, y + 14)
+    pdf.set_font("Helvetica", "B", 12)
+    pdf.cell(95, 7, "Sana:")
+    pdf.set_font("Helvetica", "", 12)
+    pdf.cell(95, 7, datetime.now().strftime("%d.%m.%Y"))
+
+    # Overall band (average of graded sections)
+    graded_bands = [s["band"] for s in sections if s.get("band") is not None]
+    overall = round(sum(graded_bands) / len(graded_bands), 1) if graded_bands else None
+
+    y = 84
+    pdf.set_fill_color(238, 243, 255)
+    pdf.rect(10, y, 190, 26, style="F")
+    pdf.set_xy(10, y + 4)
+    pdf.set_font("Helvetica", "B", 11)
+    pdf.set_text_color(74, 89, 120)
+    pdf.cell(190, 6, "UMUMIY BAND SCORE", align="C")
+    pdf.set_xy(10, y + 10)
+    pdf.set_font("Helvetica", "B", 26)
+    pdf.set_text_color(26, 86, 232)
+    pdf.cell(190, 12, str(overall) if overall is not None else "Kutilmoqda", align="C")
+
+    # Section breakdown table
+    y = 118
+    pdf.set_text_color(11, 23, 51)
+    pdf.set_font("Helvetica", "B", 12)
+    pdf.set_xy(10, y)
+    pdf.cell(190, 8, "Bo'limlar bo'yicha natija")
+
+    y += 10
+    section_labels = {"listening": "Listening", "reading": "Reading", "writing": "Writing"}
+    pdf.set_fill_color(26, 86, 232)
+    pdf.set_text_color(255, 255, 255)
+    pdf.set_font("Helvetica", "B", 10)
+    pdf.set_xy(10, y)
+    pdf.cell(63, 9, "Bo'lim", border=1, fill=True, align="C")
+    pdf.cell(63, 9, "Natija", border=1, fill=True, align="C")
+    pdf.cell(64, 9, "Band", border=1, fill=True, align="C")
+
+    pdf.set_text_color(11, 23, 51)
+    pdf.set_font("Helvetica", "", 10)
+    for s in sections:
+        y += 9
+        pdf.set_xy(10, y)
+        label = section_labels.get(s["section"], s["section"])
+        if s["section"] == "writing":
+            detail = "Tekshirilmoqda" if s.get("band") is None else "Baholandi"
+        else:
+            detail = f'{s["score"]}/{s["total"]}' if s.get("score") is not None else "-"
+        band_txt = str(s["band"]) if s.get("band") is not None else "-"
+        pdf.cell(63, 9, label, border=1, align="C")
+        pdf.cell(63, 9, detail, border=1, align="C")
+        pdf.cell(64, 9, band_txt, border=1, align="C")
+
+    # Footer
+    pdf.set_xy(10, 275)
+    pdf.set_font("Helvetica", "", 8)
+    pdf.set_text_color(74, 89, 120)
+    pdf.cell(190, 5, "Bu hujjat avtomatik generatsiya qilingan - ielts.sultanov.space", align="C")
+
+    return bytes(pdf.output())
+
+
+async def apply_writing_grade(result_id: int, band: float, feedback: str = None, send_email: bool = True) -> dict:
+    """Writing natijasiga band/feedback qo'yadi, so'ng (agar so'ralsa) email+PDF+Telegram orqali
+    xabar beradi. Ruxsat tekshiruvi (bu natija chaqiruvchiga tegishlimi) chaqiruvchi tomonda
+    (admin yoki teacher route) allaqachon bajarilgan bo'lishi kerak — bu funksiya faqat baholash
+    va bildirishnoma mantig'ini bajaradi."""
+    db = await get_pool()
+    async with db.acquire() as conn:
+        row = await conn.fetchrow(
+            """
+            UPDATE exam_results
+            SET writing_band = $1, writing_feedback = $2
+            WHERE id = $3
+            RETURNING full_name, email, section
+            """,
+            band, feedback, result_id
+        )
+        if not row:
+            raise HTTPException(status_code=404, detail="Natija topilmadi")
+
+    if not send_email:
+        return {"message": "Baho saqlandi"}
+
+    try:
+        html = build_result_email(row["full_name"], "writing", None, None, band, feedback)
+
+        async with db.acquire() as conn:
+            rows = await conn.fetch(
+                """
+                SELECT DISTINCT ON (section) full_name, section, score, total, writing_band
+                FROM exam_results
+                WHERE email = $1
+                ORDER BY section, submitted_at DESC
+                """,
+                row["email"].lower()
+            )
+        sections = []
+        for r in rows:
+            if r["section"] == "writing":
+                r_band = float(r["writing_band"]) if r["writing_band"] is not None else None
+            else:
+                r_band = get_band_score(r["score"], r["total"], r["section"])
+            sections.append({
+                "section": r["section"],
+                "score": r["score"],
+                "total": r["total"],
+                "band": r_band
+            })
+
+        pdf_bytes = build_result_pdf(row["full_name"], row["email"].lower(), sections)
+        await send_email_with_attachment(
+            row["email"], row["full_name"], "IELTS Mock — Writing natijangiz tayyor",
+            html, pdf_bytes, f"ielts_natija_{row['email'].lower().split('@')[0]}.pdf"
+        )
+
+        async with db.acquire() as conn:
+            await conn.execute("UPDATE exam_results SET notified = TRUE WHERE id = $1", result_id)
+            user_row = await conn.fetchrow("SELECT telegram_chat_id FROM users WHERE email = $1", row["email"].lower())
+
+        if user_row and user_row["telegram_chat_id"]:
+            from telegram import notify_user_writing_graded
+            await notify_user_writing_graded(user_row["telegram_chat_id"], row["full_name"], band, feedback)
+
+    except Exception as e:
+        return {"message": "Baho saqlandi, lekin email yuborilmadi", "error": str(e)}
+
+    return {"message": "Baho saqlandi va email yuborildi"}
diff --git a/groups_db.py b/groups_db.py
index e118701..230d625 100644
--- a/groups_db.py
+++ b/groups_db.py
@@ -10,6 +10,7 @@ from db import get_pool
 # uchun alohida qiymat o'rnatilishi mumkin (NULL bo'lsa — shu default ishlatiladi).
 DEFAULT_MAX_GROUPS_PER_CENTER = 10
 DEFAULT_MAX_STUDENTS_PER_CENTER = 500
+TEACHER_INVITE_TTL_DAYS = 7  # teacher-invite link shuncha kun amal qiladi, keyin avtomatik eskiradi
 
 
 async def ensure_center_group_tables():
@@ -33,12 +34,14 @@ async def ensure_center_group_tables():
                 name TEXT NOT NULL,
                 invite_code TEXT UNIQUE NOT NULL,
                 teacher_invite_code TEXT UNIQUE,
+                teacher_invite_expires_at TIMESTAMP,
                 teacher_id INTEGER REFERENCES users(id) ON DELETE SET NULL,
                 center_id INTEGER REFERENCES centers(id) ON DELETE CASCADE,
                 is_active BOOLEAN DEFAULT TRUE,
                 created_at TIMESTAMP DEFAULT NOW()
             )
         """)
+        await conn.execute("ALTER TABLE groups ADD COLUMN IF NOT EXISTS teacher_invite_expires_at TIMESTAMP")
 
         # PostgreSQL "ADD CONSTRAINT IF NOT EXISTS"ni qo'llab-quvvatlamaydi,
         # shuning uchun pg_constraint katalogini o'zimiz tekshiramiz.
diff --git a/head_teacher_routes.py b/head_teacher_routes.py
index 8776eb2..42499c6 100644
--- a/head_teacher_routes.py
+++ b/head_teacher_routes.py
@@ -1,14 +1,16 @@
 import csv
 import io
 import secrets
+from datetime import datetime, timedelta
 
 from fastapi import APIRouter, Depends, HTTPException, UploadFile, File
 from pydantic import BaseModel
 
 from db import get_pool
 from auth import get_current_head_teacher, FRONTEND_BASE_URL
-from groups_db import DEFAULT_MAX_GROUPS_PER_CENTER, DEFAULT_MAX_STUDENTS_PER_CENTER
+from groups_db import DEFAULT_MAX_GROUPS_PER_CENTER, DEFAULT_MAX_STUDENTS_PER_CENTER, TEACHER_INVITE_TTL_DAYS
 from scoring import get_band_score
+from exports import export_rows
 
 router = APIRouter(prefix="/api/head-teacher", tags=["head-teacher"])
 
@@ -56,6 +58,7 @@ async def list_groups(current_user: dict = Depends(get_current_head_teacher)):
             """
             SELECT g.id, g.name, g.invite_code, g.is_active, g.created_at,
                    g.teacher_id, t.full_name AS teacher_name, t.email AS teacher_email,
+                   g.teacher_invite_code, g.teacher_invite_expires_at,
                    COUNT(u.id) FILTER (WHERE u.role = 'student') AS students_count
             FROM groups g
             LEFT JOIN users t ON t.id = g.teacher_id
@@ -66,16 +69,23 @@ async def list_groups(current_user: dict = Depends(get_current_head_teacher)):
             """,
             current_user["center_id"]
         )
-    return [
-        {
+    now = datetime.utcnow()
+    result = []
+    for r in rows:
+        expires_at = r["teacher_invite_expires_at"]
+        invite_expired = bool(expires_at and expires_at <= now)
+        has_pending_invite = bool(r["teacher_invite_code"]) and not invite_expired
+        result.append({
             "id": r["id"], "name": r["name"], "invite_code": r["invite_code"],
             "is_active": r["is_active"], "created_at": r["created_at"].isoformat(),
             "has_teacher": r["teacher_id"] is not None,
             "teacher_name": r["teacher_name"], "teacher_email": r["teacher_email"],
             "students_count": r["students_count"],
-        }
-        for r in rows
-    ]
+            "has_pending_invite": has_pending_invite,
+            "invite_expired": invite_expired,
+            "teacher_invite_expires_at": expires_at.isoformat() if expires_at else None,
+        })
+    return result
 
 
 @router.post("/groups")
@@ -136,16 +146,39 @@ async def generate_teacher_invite(group_id: int, current_user: dict = Depends(ge
             )
 
         invite_code = secrets.token_urlsafe(24)  # yuqori entropiya — bu rol beruvchi token
+        expires_at = datetime.utcnow() + timedelta(days=TEACHER_INVITE_TTL_DAYS)
         await conn.execute(
-            "UPDATE groups SET teacher_invite_code=$1 WHERE id=$2", invite_code, group_id
+            "UPDATE groups SET teacher_invite_code=$1, teacher_invite_expires_at=$2 WHERE id=$3",
+            invite_code, expires_at, group_id
         )
 
     return {
         "teacher_invite_code": invite_code,
         "invite_link": f"{FRONTEND_BASE_URL}/register?teacher_invite={invite_code}",
+        "expires_at": expires_at.isoformat(),
+        "ttl_days": TEACHER_INVITE_TTL_DAYS,
     }
 
 
+@router.post("/groups/{group_id}/revoke-teacher-invite")
+async def revoke_teacher_invite(group_id: int, current_user: dict = Depends(get_current_head_teacher)):
+    """Hali ishlatilmagan teacher-invite linkni bekor qilish — masalan, link noto'g'ri
+    odamga yuborilib qolgan yoki eskirgan bo'lsa. Agar link allaqachon ishlatilib
+    teacher tayinlangan bo'lsa, buning o'rniga remove-teacher ishlatilishi kerak."""
+    db = await get_pool()
+    async with db.acquire() as conn:
+        group = await _own_group_or_404(conn, group_id, current_user["center_id"])
+        if not group["teacher_invite_code"]:
+            raise HTTPException(status_code=400, detail="Bu guruh uchun faol taklif linki yo'q")
+
+        await conn.execute(
+            "UPDATE groups SET teacher_invite_code=NULL, teacher_invite_expires_at=NULL WHERE id=$1",
+            group_id
+        )
+
+    return {"message": "Taklif linki bekor qilindi"}
+
+
 @router.post("/groups/{group_id}/remove-teacher")
 async def remove_teacher(group_id: int, current_user: dict = Depends(get_current_head_teacher)):
     db = await get_pool()
@@ -159,7 +192,7 @@ async def remove_teacher(group_id: int, current_user: dict = Depends(get_current
                 "UPDATE users SET role='student', center_id=NULL WHERE id=$1", group["teacher_id"]
             )
             await conn.execute(
-                "UPDATE groups SET teacher_id=NULL, teacher_invite_code=NULL WHERE id=$1", group_id
+                "UPDATE groups SET teacher_id=NULL, teacher_invite_code=NULL, teacher_invite_expires_at=NULL WHERE id=$1", group_id
             )
 
     return {"message": "Teacher guruhdan olib tashlandi, hisobi student sifatida saqlanib qoldi"}
@@ -265,6 +298,19 @@ async def head_teacher_stats(current_user: dict = Depends(get_current_head_teach
             current_user["center_id"]
         )
 
+        daily_rows = await conn.fetch(
+            """
+            SELECT DATE(er.submitted_at) AS date, COUNT(DISTINCT er.email) AS count
+            FROM exam_results er
+            JOIN users u ON u.email = er.email
+            WHERE u.center_id = $1 AND u.role = 'student'
+              AND er.submitted_at >= NOW() - INTERVAL '30 days'
+            GROUP BY DATE(er.submitted_at)
+            ORDER BY date
+            """,
+            current_user["center_id"]
+        )
+
     bands_by_section = {}
     for r in rows:
         if r["section"] == "writing":
@@ -281,4 +327,46 @@ async def head_teacher_stats(current_user: dict = Depends(get_current_head_teach
         "average_band_by_section": averages,
         "weakest_section": weakest_section,
         "total_attempts": len(rows),
+        "daily_activity": [{"date": str(r["date"]), "count": r["count"]} for r in daily_rows],
     }
+
+
+# ─── Export ──────────────────────────────────────────────────────────────────
+
+@router.get("/export/results")
+async def export_center_results(format: str = "csv", current_user: dict = Depends(get_current_head_teacher)):
+    """Markazning barcha guruhlari bo'yicha natijalarni Excel/CSV qilib yuklab olish —
+    ota-onalarga yoki markaz hisobotlariga tayyor holda beriladi."""
+    db = await get_pool()
+    async with db.acquire() as conn:
+        rows = await conn.fetch(
+            """
+            SELECT er.id, er.full_name, er.email, g.name AS group_name, er.section,
+                   er.score, er.total, er.writing_band, er.submitted_at
+            FROM exam_results er
+            JOIN users u ON u.email = er.email
+            LEFT JOIN groups g ON g.id = u.group_id
+            WHERE u.center_id = $1 AND u.role = 'student'
+            ORDER BY er.submitted_at DESC
+            """,
+            current_user["center_id"]
+        )
+
+    data = []
+    for r in rows:
+        if r["section"] == "writing":
+            band = float(r["writing_band"]) if r["writing_band"] is not None else ""
+        else:
+            band = get_band_score(r["score"], r["total"], r["section"]) or ""
+        data.append({
+            "ID": r["id"],
+            "Ism": r["full_name"],
+            "Email": r["email"],
+            "Guruh": r["group_name"] or "",
+            "Bo'lim": r["section"],
+            "Natija": f"{r['score']}/{r['total']}" if r["score"] is not None else "",
+            "Band": band,
+            "Sana": r["submitted_at"].isoformat(),
+        })
+
+    return export_rows(data, "markaz_natijalar", format, "Natijalar")
diff --git a/main.py b/main.py
index 68a9096..09e1d47 100644
--- a/main.py
+++ b/main.py
@@ -7,17 +7,8 @@ from typing import Optional
 import os
 import json
 import httpx
-import asyncio
 import io
-import base64
-import csv
 import hmac
-try:
-    import openpyxl
-except ImportError:
-    openpyxl = None
-from datetime import datetime
-from fpdf import FPDF
 
 from db import get_pool
 from scoring import get_band_score
@@ -25,6 +16,8 @@ from auth import router as auth_router, ensure_users_table, get_current_user
 from groups_db import ensure_center_group_tables, DEFAULT_MAX_GROUPS_PER_CENTER, DEFAULT_MAX_STUDENTS_PER_CENTER
 from head_teacher_routes import router as head_teacher_router
 from teacher_routes import router as teacher_router
+from grading import build_result_email, build_result_pdf, send_email_with_attachment, apply_writing_grade
+from exports import export_rows
 
 app = FastAPI(title="IELTS Mock SS")
 app.include_router(feature_router)
@@ -136,65 +129,7 @@ async def send_email(to_email: str, to_name: str, subject: str, html_body: str):
         if response.status_code >= 400:
             raise RuntimeError(f"Resend xatosi: {response.text}")
 
-async def send_email_with_attachment(to_email: str, to_name: str, subject: str, html_body: str, attachment_bytes: bytes, attachment_filename: str):
-    if not RESEND_API_KEY:
-        raise RuntimeError("RESEND_API_KEY sozlanmagan")
-
-    b64_content = base64.b64encode(attachment_bytes).decode('utf-8')
-
-    async with httpx.AsyncClient(timeout=15) as client:
-        response = await client.post(
-            "https://api.resend.com/emails",
-            headers={
-                "Authorization": f"Bearer {RESEND_API_KEY}",
-                "Content-Type": "application/json"
-            },
-            json={
-                "from": f"IELTS Mock <{EMAIL_FROM}>",
-                "to": [to_email],
-                "subject": subject,
-                "html": html_body,
-                "attachments": [
-                    {
-                        "filename": attachment_filename,
-                        "content": b64_content
-                    }
-                ]
-            }
-        )
-        if response.status_code >= 400:
-            raise RuntimeError(f"Resend xatosi: {response.text}")
-
 
-def build_result_email(name: str, section: str, score, total, band, feedback=None) -> str:
-    section_names = {"listening": "Listening", "reading": "Reading", "writing": "Writing"}
-    section_name = section_names.get(section, section)
-
-    if section == "writing":
-        body = f"""
-        <p><b>Band Score:</b> {band if band is not None else 'Hali baholanmagan'}</p>
-        {f'<p><b>Izoh:</b> {feedback}</p>' if feedback else ''}
-        """
-    else:
-        body = f"""
-        <p><b>Natija:</b> {score} / {total} to'g'ri</p>
-        <p><b>Band Score:</b> {band}</p>
-        """
-
-    return f"""
-    <div style="font-family:Arial,sans-serif; max-width:520px; margin:0 auto; padding:24px; border:1px solid #c9d8ff; border-radius:12px;">
-      <h2 style="color:#1a56e8;">IELTS Mock — {section_name} natijasi</h2>
-      <p>Assalomu alaykum, {name}!</p>
-      {body}
-      <p style="margin-top:20px; color:#4a5978; font-size:13px;">
-        Batafsil: <a href="https://ielts.sultanov.space" style="color:#1a56e8;">ielts.sultanov.space</a>
-      </p>
-      <p style="color:#4a5978; font-size:12px; margin-top:16px;"> © 2026-2027 Bo'stonliq tuman ixtisoslashtirilgan maktabi</p>
-    </div>
-    """
-
-
-# ─── Endpoints ─────────────────────────────────────────────────────────────────
 
 @app.post("/api/start")
 async def start_session(data: StartSession, current_user: dict = Depends(get_current_user)):
@@ -387,113 +322,7 @@ async def get_results(email: str, current_user: dict = Depends(get_current_user)
     return {"email": email, "results": results}
 
 
-# ─── PDF Sertifikat ─────────────────────────────────────────────────────────────
-
-def build_result_pdf(full_name: str, email: str, sections: list) -> bytes:
-    pdf = FPDF(orientation="P", unit="mm", format="A4")
-    pdf.add_page()
-    pdf.set_auto_page_break(auto=False)
-
-    # Header
-    pdf.set_fill_color(26, 86, 232)
-    pdf.rect(0, 0, 210, 30, style="F")
-    pdf.set_xy(0, 9)
-    pdf.set_text_color(255, 255, 255)
-    pdf.set_font("Helvetica", "B", 18)
-    pdf.cell(210, 8, "IELTS Mock Exam - Natija", align="C")
-    pdf.set_xy(0, 18)
-    pdf.set_font("Helvetica", "", 10)
-    pdf.cell(210, 6, "ielts.sultanov.space", align="C")
-
-    # Demo disclaimer banner
-    pdf.set_xy(10, 36)
-    pdf.set_fill_color(255, 251, 235)
-    pdf.set_draw_color(217, 119, 6)
-    pdf.rect(10, 36, 190, 12, style="DF")
-    pdf.set_xy(12, 39)
-    pdf.set_text_color(217, 119, 6)
-    pdf.set_font("Helvetica", "", 9)
-    pdf.multi_cell(186, 5,
-        "Diqqat: bu DEMO test rejimi natijasi. Savollar namunaviy, haqiqiy imtihon "
-        "formatidagi savollar hali qo'shilmagan. Rasmiy sertifikat emas.")
-
-    # Candidate info
-    y = 56
-    pdf.set_text_color(11, 23, 51)
-    pdf.set_xy(10, y)
-    pdf.set_font("Helvetica", "B", 12)
-    pdf.cell(95, 7, "Ism-familiya:")
-    pdf.set_font("Helvetica", "", 12)
-    pdf.cell(95, 7, full_name)
-    pdf.set_xy(10, y + 7)
-    pdf.set_font("Helvetica", "B", 12)
-    pdf.cell(95, 7, "Email:")
-    pdf.set_font("Helvetica", "", 12)
-    pdf.cell(95, 7, email)
-    pdf.set_xy(10, y + 14)
-    pdf.set_font("Helvetica", "B", 12)
-    pdf.cell(95, 7, "Sana:")
-    pdf.set_font("Helvetica", "", 12)
-    pdf.cell(95, 7, datetime.now().strftime("%d.%m.%Y"))
-
-    # Overall band (average of graded sections)
-    graded_bands = [s["band"] for s in sections if s.get("band") is not None]
-    overall = round(sum(graded_bands) / len(graded_bands), 1) if graded_bands else None
-
-    y = 84
-    pdf.set_fill_color(238, 243, 255)
-    pdf.rect(10, y, 190, 26, style="F")
-    pdf.set_xy(10, y + 4)
-    pdf.set_font("Helvetica", "B", 11)
-    pdf.set_text_color(74, 89, 120)
-    pdf.cell(190, 6, "UMUMIY BAND SCORE", align="C")
-    pdf.set_xy(10, y + 10)
-    pdf.set_font("Helvetica", "B", 26)
-    pdf.set_text_color(26, 86, 232)
-    pdf.cell(190, 12, str(overall) if overall is not None else "Kutilmoqda", align="C")
-
-    # Section breakdown table
-    y = 118
-    pdf.set_text_color(11, 23, 51)
-    pdf.set_font("Helvetica", "B", 12)
-    pdf.set_xy(10, y)
-    pdf.cell(190, 8, "Bo'limlar bo'yicha natija")
-
-    y += 10
-    section_labels = {"listening": "Listening", "reading": "Reading", "writing": "Writing"}
-    pdf.set_fill_color(26, 86, 232)
-    pdf.set_text_color(255, 255, 255)
-    pdf.set_font("Helvetica", "B", 10)
-    pdf.set_xy(10, y)
-    pdf.cell(63, 9, "Bo'lim", border=1, fill=True, align="C")
-    pdf.cell(63, 9, "Natija", border=1, fill=True, align="C")
-    pdf.cell(64, 9, "Band", border=1, fill=True, align="C")
-
-    pdf.set_text_color(11, 23, 51)
-    pdf.set_font("Helvetica", "", 10)
-    for s in sections:
-        y += 9
-        pdf.set_xy(10, y)
-        label = section_labels.get(s["section"], s["section"])
-        if s["section"] == "writing":
-            detail = "Tekshirilmoqda" if s.get("band") is None else "Admin tomonidan baholandi"
-        else:
-            detail = f'{s["score"]}/{s["total"]}' if s.get("score") is not None else "-"
-        band_txt = str(s["band"]) if s.get("band") is not None else "-"
-        pdf.cell(63, 9, label, border=1, align="C")
-        pdf.cell(63, 9, detail, border=1, align="C")
-        pdf.cell(64, 9, band_txt, border=1, align="C")
-
-    # Footer
-    pdf.set_xy(10, 275)
-    pdf.set_font("Helvetica", "", 8)
-    pdf.set_text_color(74, 89, 120)
-    pdf.cell(190, 5, "Bu hujjat avtomatik generatsiya qilingan - ielts.sultanov.space", align="C")
-
-    return bytes(pdf.output())
 
-
-@app.get("/api/results/{email}/pdf")
 async def get_results_pdf(email: str, current_user: dict = Depends(get_current_user)):
     if email.strip().lower() != current_user["email"].strip().lower():
         raise HTTPException(status_code=403, detail="Faqat o'zingizning natijalaringizni yuklab olishingiz mumkin")
@@ -562,6 +391,7 @@ async def admin_results(_: None = Depends(require_admin)):
         rows = await conn.fetch(
             """
             SELECT id, full_name, email, section, score, total,
+                   writing_task1, writing_task2,
                    writing_band, writing_feedback, notified, submitted_at
             FROM exam_results
             ORDER BY submitted_at DESC
@@ -578,6 +408,8 @@ async def admin_results(_: None = Depends(require_admin)):
                 "section": r["section"],
                 "score": r["score"],
                 "total": r["total"],
+                "writing_task1": r["writing_task1"],
+                "writing_task2": r["writing_task2"],
                 "band": (
                     float(r["writing_band"]) if r["section"] == "writing" and r["writing_band"] is not None
                     else get_band_score(r["score"], r["total"], r["section"]) if r["section"] != "writing"
@@ -763,65 +595,7 @@ async def admin_delete_user(user_id: int, _: None = Depends(require_admin)):
 
 @app.post("/api/admin/grade-writing")
 async def grade_writing(data: GradeWriting, _: None = Depends(require_admin)):
-    db = await get_pool()
-    async with db.acquire() as conn:
-        row = await conn.fetchrow(
-            """
-            UPDATE exam_results
-            SET writing_band = $1, writing_feedback = $2
-            WHERE id = $3
-            RETURNING full_name, email, section
-            """,
-            data.band, data.feedback, data.result_id
-        )
-        if not row:
-            raise HTTPException(status_code=404, detail="Natija topilmadi")
-
-    if data.send_email:
-        try:
-            html = build_result_email(row["full_name"], "writing", None, None, data.band, data.feedback)
-            
-            async with db.acquire() as conn:
-                rows = await conn.fetch(
-                    """
-                    SELECT DISTINCT ON (section) full_name, section, score, total, writing_band
-                    FROM exam_results
-                    WHERE email = $1
-                    ORDER BY section, submitted_at DESC
-                    """,
-                    row["email"].lower()
-                )
-            sections = []
-            for r in rows:
-                if r["section"] == "writing":
-                    r_band = float(r["writing_band"]) if r["writing_band"] is not None else None
-                else:
-                    r_band = get_band_score(r["score"], r["total"], r["section"])
-                sections.append({
-                    "section": r["section"],
-                    "score": r["score"],
-                    "total": r["total"],
-                    "band": r_band
-                })
-                
-            pdf_bytes = build_result_pdf(row["full_name"], row["email"].lower(), sections)
-            await send_email_with_attachment(
-                row["email"], row["full_name"], "IELTS Mock — Writing natijangiz tayyor", 
-                html, pdf_bytes, f"ielts_natija_{row['email'].lower().split('@')[0]}.pdf"
-            )
-            
-            async with db.acquire() as conn:
-                await conn.execute("UPDATE exam_results SET notified = TRUE WHERE id = $1", data.result_id)
-                user_row = await conn.fetchrow("SELECT telegram_chat_id FROM users WHERE email = $1", row["email"].lower())
-                
-            if user_row and user_row["telegram_chat_id"]:
-                from telegram import notify_user_writing_graded
-                await notify_user_writing_graded(user_row["telegram_chat_id"], row["full_name"], data.band, data.feedback)
-                
-        except Exception as e:
-            return {"message": "Baho saqlandi, lekin email yuborilmadi", "error": str(e)}
-
-    return {"message": "Baho saqlandi va email yuborildi"}
+    return await apply_writing_grade(data.result_id, data.band, data.feedback, data.send_email)
 
 
 @app.post("/api/admin/notify/{result_id}")
@@ -958,7 +732,7 @@ async def export_results(format: str = "csv", _: None = Depends(require_admin)):
             ORDER BY submitted_at DESC
             """
         )
-        
+
     data = []
     for r in rows:
         b = float(r["writing_band"]) if r["section"] == "writing" and r["writing_band"] is not None else get_band_score(r["score"], r["total"], r["section"]) if r["section"] != "writing" else ""
@@ -971,37 +745,8 @@ async def export_results(format: str = "csv", _: None = Depends(require_admin)):
             "Band": b,
             "Sana": r["submitted_at"].isoformat()
         })
-        
-    if format == "csv":
-        output = io.StringIO()
-        if data:
-            writer = csv.DictWriter(output, fieldnames=data[0].keys())
-            writer.writeheader()
-            writer.writerows(data)
-        output.seek(0)
-        return StreamingResponse(
-            iter([b'\xef\xbb\xbf' + output.getvalue().encode('utf-8')]),
-            media_type="text/csv",
-            headers={"Content-Disposition": "attachment; filename=results.csv"}
-        )
-    elif format == "excel":
-        if not openpyxl:
-            raise HTTPException(status_code=500, detail="openpyxl o'rnatilmagan")
-        wb = openpyxl.Workbook()
-        ws = wb.active
-        ws.title = "Results"
-        if data:
-            ws.append(list(data[0].keys()))
-            for d in data:
-                ws.append(list(d.values()))
-        output = io.BytesIO()
-        wb.save(output)
-        output.seek(0)
-        return StreamingResponse(
-            output,
-            media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
-            headers={"Content-Disposition": "attachment; filename=results.xlsx"}
-        )
+
+    return export_rows(data, "results", format, "Results")
 
 @app.get("/api/admin/export/users")
 async def export_users(format: str = "csv", _: None = Depends(require_admin)):
@@ -1018,7 +763,7 @@ async def export_users(format: str = "csv", _: None = Depends(require_admin)):
             ORDER BY u.created_at DESC
             """
         )
-        
+
     data = []
     for r in rows:
         data.append({
@@ -1033,37 +778,8 @@ async def export_users(format: str = "csv", _: None = Depends(require_admin)):
             "Telegram": r["telegram_chat_id"] or "",
             "Taklif qildi": r["referral_count"] or 0
         })
-        
-    if format == "csv":
-        output = io.StringIO()
-        if data:
-            writer = csv.DictWriter(output, fieldnames=data[0].keys())
-            writer.writeheader()
-            writer.writerows(data)
-        output.seek(0)
-        return StreamingResponse(
-            iter([b'\xef\xbb\xbf' + output.getvalue().encode('utf-8')]),
-            media_type="text/csv",
-            headers={"Content-Disposition": "attachment; filename=users.csv"}
-        )
-    elif format == "excel":
-        if not openpyxl:
-            raise HTTPException(status_code=500, detail="openpyxl o'rnatilmagan")
-        wb = openpyxl.Workbook()
-        ws = wb.active
-        ws.title = "Users"
-        if data:
-            ws.append(list(data[0].keys()))
-            for d in data:
-                ws.append(list(d.values()))
-        output = io.BytesIO()
-        wb.save(output)
-        output.seek(0)
-        return StreamingResponse(
-            output,
-            media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
-            headers={"Content-Disposition": "attachment; filename=users.xlsx"}
-        )
+
+    return export_rows(data, "users", format, "Users")
 
 
 # ─── Static Files ──────────────────────────────────────────────────────────────
diff --git a/static/admin.html b/static/admin.html
index 864d731..04ff3c0 100644
--- a/static/admin.html
+++ b/static/admin.html
@@ -98,6 +98,10 @@ input:focus,textarea:focus{border-color:var(--blue);box-shadow:0 0 0 3px rgba(26
   border-top:1px solid var(--border);background:var(--bg);}
 .grade-form > div{flex:1;min-width:120px;}
 .grade-form textarea{min-height:60px;resize:vertical;}
+.essay-label{font-family:var(--mono);font-size:11px;color:var(--muted);text-transform:uppercase;
+  letter-spacing:.04em;margin-bottom:4px;}
+.essay-box{background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:12px 14px;
+  margin-bottom:10px;font-size:13px;line-height:1.6;white-space:pre-wrap;max-height:220px;overflow-y:auto;}
 
 .spinner{width:16px;height:16px;border:2px solid var(--border);border-top-color:var(--blue);
   border-radius:50%;animation:spin .7s linear infinite;display:inline-block;}
@@ -591,7 +595,12 @@ function renderResults() {
         scoreHtml = `<span class="result-score">${r.band}</span><span class="badge muted">Band</span>`;
       } else {
         scoreHtml = `<span class="badge amber">Baholanmagan</span>`;
+        const essayHtml = `
+          ${r.writing_task1 ? `<div class="essay-label">Task 1</div><div class="essay-box">${escapeHtml(r.writing_task1)}</div>` : ''}
+          ${r.writing_task2 ? `<div class="essay-label">Task 2</div><div class="essay-box">${escapeHtml(r.writing_task2)}</div>` : ''}
+        `;
         gradeForm = `
+        <div style="padding:16px 20px 0;">${essayHtml}</div>
         <div class="grade-form">
           <div>
             <label>Band (0-9)</label>
@@ -635,6 +644,12 @@ function renderResults() {
   }).join('');
 }
 
+function escapeHtml(text) {
+  const div = document.createElement('div');
+  div.textContent = text || '';
+  return div.innerHTML;
+}
+
 async function gradeWriting(id) {
   const band = document.getElementById(`band-${id}`).value;
   const feedback = document.getElementById(`feedback-${id}`).value.trim();
diff --git a/static/head-teacher.html b/static/head-teacher.html
index 72d130f..ea5d4d2 100644
--- a/static/head-teacher.html
+++ b/static/head-teacher.html
@@ -6,6 +6,7 @@
 <title>Markaz Paneli — IELTS Mock SS</title>
 <meta name="robots" content="noindex, nofollow">
 <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
+<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
 <style>
 :root {
   --blue:       #1a56e8;
@@ -193,7 +194,13 @@ input:focus, textarea:focus { border-color: var(--blue); box-shadow: 0 0 0 3px r
     <!-- GROUPS -->
     <div class="section-view active" id="section-groups">
       <div class="dash-section">
-        <div class="dash-section-head"><span>$ guruh --create</span></div>
+        <div class="dash-section-head">
+          <span>$ guruh --create</span>
+          <div style="display:flex; gap:8px;">
+            <button class="btn-outline btn-sm" onclick="exportResults('csv')">⬇ CSV</button>
+            <button class="btn-outline btn-sm" onclick="exportResults('excel')">⬇ Excel</button>
+          </div>
+        </div>
         <div class="dash-section-body" style="display:flex; gap:10px; flex-wrap:wrap; align-items:flex-end;">
           <div style="flex:1; min-width:200px;">
             <label>Guruh nomi</label>
@@ -213,6 +220,7 @@ input:focus, textarea:focus { border-color: var(--blue); box-shadow: 0 0 0 3px r
         <div class="dash-section-head"><span>$ markaz --stats</span></div>
         <div class="dash-section-body">
           <div id="stats-body"></div>
+          <div style="height:250px; margin-top:16px;"><canvas id="activityChart"></canvas></div>
         </div>
       </div>
     </div>
@@ -382,15 +390,27 @@ function renderGroups() {
 }
 
 function renderGroupExpandPanel(g) {
+  let inviteSection = '';
+  if (g.has_teacher) {
+    inviteSection = `<button class="btn-outline btn-sm danger" onclick="removeTeacher(${g.id}, this)">🗑 Teacherni olib tashlash</button>`;
+  } else if (g.has_pending_invite) {
+    const expiresText = g.teacher_invite_expires_at
+      ? new Date(g.teacher_invite_expires_at).toLocaleString('uz-UZ', {day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})
+      : '';
+    inviteSection = `
+      <button class="btn-outline btn-sm danger" onclick="revokeInvite(${g.id}, this)">✕ Taklifni bekor qilish</button>
+      <span class="badge muted">amal qilish muddati: ${expiresText}</span>
+    `;
+  } else {
+    inviteSection = `<button class="btn-outline btn-sm" onclick="generateInvite(${g.id}, this)">🔗 Teacher-invite yaratish</button>`;
+  }
+
   return `
     <div>
       <div style="font-family:var(--mono); font-size:11px; color:var(--muted); margin-bottom:6px;">$ amallar</div>
       <div class="group-actions">
         <button class="btn-outline btn-sm" onclick="toggleGroupActive(${g.id}, this)">${g.is_active ? '⛔ Guruhni yopish' : '✓ Qayta ochish'}</button>
-        ${g.has_teacher
-          ? `<button class="btn-outline btn-sm danger" onclick="removeTeacher(${g.id}, this)">🗑 Teacherni olib tashlash</button>`
-          : `<button class="btn-outline btn-sm" onclick="generateInvite(${g.id}, this)">🔗 Teacher-invite yaratish</button>`
-        }
+        ${inviteSection}
         <button class="btn-outline btn-sm" onclick="viewRoster(${g.id})">📋 Ro'yxat (roster)</button>
       </div>
     </div>
@@ -473,13 +493,15 @@ async function generateInvite(groupId, btnEl) {
   btnEl.innerHTML = '<span class="spinner"></span>';
   try {
     const data = await apiCall(HT_API, `/groups/${groupId}/generate-teacher-invite`, { method: 'POST' });
+    const expiresText = new Date(data.expires_at).toLocaleString('uz-UZ', {day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});
     document.getElementById(`invite-box-${groupId}`).innerHTML = `
-      <div style="font-family:var(--mono); font-size:11px; color:var(--muted); margin-bottom:6px;">$ teacher --invite-link (bir martalik, faqat shu guruh uchun)</div>
+      <div style="font-family:var(--mono); font-size:11px; color:var(--muted); margin-bottom:6px;">$ teacher --invite-link (bir martalik, faqat shu guruh uchun, ${data.ttl_days} kun amal qiladi — ${expiresText} gacha)</div>
       <div class="code-box">
         <span>${data.invite_link}</span>
         <button class="btn-outline btn-sm" onclick="copyText('${data.invite_link}', this)">📋 Nusxa</button>
       </div>
     `;
+    await loadGroups();
   } catch (e) {
     alert('Xatolik: ' + e.message);
   } finally {
@@ -488,6 +510,19 @@ async function generateInvite(groupId, btnEl) {
   }
 }
 
+async function revokeInvite(groupId, btnEl) {
+  if (!confirm("Bu taklif linkini bekor qilmoqchimisiz? Link boshqa ishlamaydi.")) return;
+  btnEl.disabled = true;
+  try {
+    await apiCall(HT_API, `/groups/${groupId}/revoke-teacher-invite`, { method: 'POST' });
+    document.getElementById(`invite-box-${groupId}`).innerHTML = '';
+    await loadGroups();
+  } catch (e) {
+    alert('Xatolik: ' + e.message);
+    btnEl.disabled = false;
+  }
+}
+
 function copyText(text, btnEl) {
   navigator.clipboard.writeText(text).then(() => {
     const original = btnEl.innerHTML;
@@ -581,11 +616,55 @@ async function loadStats() {
         ${data.weakest_section ? ` · Eng past ko'rsatkich: <span class="badge warning">${sectionNames[data.weakest_section] || data.weakest_section}</span>` : ''}
       </div>
     `;
+    const daily = data.daily_activity || [];
+    renderTrendChart('activityChart', daily.map(d => d.date), daily.map(d => d.count));
   } catch (e) {
     body.innerHTML = `<div class="empty-state">→ ${e.message}</div>`;
   }
 }
 
+let charts = {};
+function renderTrendChart(id, labels, counts) {
+  const canvas = document.getElementById(id);
+  if (!canvas) return;
+  if (charts[id]) charts[id].destroy();
+  charts[id] = new Chart(canvas.getContext('2d'), {
+    type: 'line',
+    data: {
+      labels,
+      datasets: [{
+        label: "Faol talabalar (kunlik)", data: counts,
+        borderColor: '#1a56e8', backgroundColor: 'rgba(26,86,232,0.1)', fill: true, tension: 0.1
+      }]
+    },
+    options: { responsive: true, maintainAspectRatio: false }
+  });
+}
+
+// ─── EXPORT ───────────────────────────────────────────────
+async function exportResults(format) {
+  try {
+    const res = await fetch(`${HT_API}/export/results?format=${format}`, {
+      headers: token ? { 'Authorization': `Bearer ${token}` } : {}
+    });
+    if (!res.ok) {
+      const data = await res.json().catch(() => ({}));
+      throw new Error(data.detail || 'Export xatosi');
+    }
+    const blob = await res.blob();
+    const url = URL.createObjectURL(blob);
+    const a = document.createElement('a');
+    a.href = url;
+    a.download = `markaz_natijalar.${format === 'excel' ? 'xlsx' : 'csv'}`;
+    document.body.appendChild(a);
+    a.click();
+    a.remove();
+    URL.revokeObjectURL(url);
+  } catch (e) {
+    alert('Xatolik: ' + e.message);
+  }
+}
+
 // ─── INIT ─────────────────────────────────────────────────
 (async function init() {
   if (!token) { showView('view-auth'); return; }
diff --git a/static/teacher.html b/static/teacher.html
index ea03690..50deccf 100644
--- a/static/teacher.html
+++ b/static/teacher.html
@@ -6,6 +6,7 @@
 <title>Teacher Panel — IELTS Mock SS</title>
 <meta name="robots" content="noindex, nofollow">
 <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
+<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
 <style>
 :root {
   --blue:       #1a56e8;
@@ -130,6 +131,31 @@ table.students-table tr.clickable:hover { background: var(--bg); }
   table.students-table { min-width: 560px; }
 }
 
+/* WRITING GRADING */
+.filters { display: flex; gap: 10px; margin-bottom: 16px; flex-wrap: wrap; align-items: center; }
+.filter-btn { font-family: var(--mono); font-size: 12px; padding: 7px 14px; border-radius: 6px;
+  border: 1px solid var(--border); background: var(--surface); cursor: pointer; color: var(--muted); }
+.filter-btn.active { background: var(--blue); color: #fff; border-color: var(--blue); }
+
+.result-card { background: var(--surface); border: 1px solid var(--border); border-radius: 12px;
+  overflow: hidden; margin-bottom: 14px; }
+.result-head { padding: 14px 20px; border-bottom: 1px solid var(--border); background: var(--surface2);
+  display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 8px; }
+.result-name { font-size: 14px; font-weight: 700; }
+.result-email { font-size: 12px; color: var(--muted); font-family: var(--mono); }
+.result-body { padding: 16px 20px; }
+.essay-label { font-family: var(--mono); font-size: 11px; color: var(--muted); text-transform: uppercase;
+  letter-spacing: .04em; margin-bottom: 4px; }
+.essay-box { background: var(--bg); border: 1px solid var(--border); border-radius: 8px; padding: 12px 14px;
+  margin-bottom: 12px; font-size: 13px; line-height: 1.6; white-space: pre-wrap; max-height: 220px; overflow-y: auto; }
+.grade-form { display: flex; gap: 10px; align-items: flex-end; flex-wrap: wrap; padding: 16px 20px;
+  border-top: 1px solid var(--border); background: var(--bg); }
+.grade-form > div { flex: 1; min-width: 120px; }
+.grade-form textarea { min-height: 60px; resize: vertical; font-family: var(--sans); width: 100%;
+  background: var(--surface); border: 1.5px solid var(--border); border-radius: 7px; padding: 10px 12px;
+  font-size: 13px; outline: none; }
+.export-row { display: flex; gap: 8px; }
+
 /* MODAL */
 .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(11,23,51,0.45);
   z-index: 100; align-items: center; justify-content: center; padding: 20px; }
@@ -213,11 +239,16 @@ table.students-table tr.clickable:hover { background: var(--bg); }
 
     <div class="section-tabs">
       <button class="section-tab active" id="tab-students" onclick="switchSection('students')">Talabalar</button>
+      <button class="section-tab" id="tab-writing" onclick="switchSection('writing')">Writing baholash</button>
       <button class="section-tab" id="tab-stats" onclick="switchSection('stats')">Statistika</button>
     </div>
 
     <!-- STUDENTS -->
     <div class="section-view active" id="section-students">
+      <div style="display:flex; justify-content:flex-end; margin-bottom:12px;" class="export-row">
+        <button class="btn-outline btn-sm" onclick="exportResults('csv')">⬇ CSV</button>
+        <button class="btn-outline btn-sm" onclick="exportResults('excel')">⬇ Excel</button>
+      </div>
       <div class="students-table-wrap">
         <table class="students-table">
           <thead>
@@ -234,12 +265,24 @@ table.students-table tr.clickable:hover { background: var(--bg); }
       <div id="students-empty" class="empty-state" style="display:none;">Guruhda hali talabalar yo'q</div>
     </div>
 
+    <!-- WRITING GRADING -->
+    <div class="section-view" id="section-writing">
+      <div class="filters">
+        <button class="filter-btn active" data-wfilter="pending" onclick="setWritingFilter('pending')">✍️ Baholanmagan</button>
+        <button class="filter-btn" data-wfilter="graded" onclick="setWritingFilter('graded')">✓ Baholangan</button>
+        <button class="filter-btn" data-wfilter="all" onclick="setWritingFilter('all')">Barchasi</button>
+      </div>
+      <div id="writing-list"></div>
+      <div id="writing-empty" class="empty-state" style="display:none;"></div>
+    </div>
+
     <!-- STATS -->
     <div class="section-view" id="section-stats">
       <div class="dash-section">
         <div class="dash-section-head"><span>$ guruh --stats</span></div>
         <div class="dash-section-body">
           <div id="stats-body"></div>
+          <div style="height:220px; margin-top:16px;"><canvas id="activityChart"></canvas></div>
         </div>
       </div>
     </div>
@@ -441,10 +484,25 @@ function closeModal() {
 // ─── STATS ────────────────────────────────────────────────
 function switchSection(which) {
   document.getElementById('tab-students').classList.toggle('active', which === 'students');
+  document.getElementById('tab-writing').classList.toggle('active', which === 'writing');
   document.getElementById('tab-stats').classList.toggle('active', which === 'stats');
   document.getElementById('section-students').classList.toggle('active', which === 'students');
+  document.getElementById('section-writing').classList.toggle('active', which === 'writing');
   document.getElementById('section-stats').classList.toggle('active', which === 'stats');
   if (which === 'stats') loadStats();
+  if (which === 'writing') loadWriting();
+}
+
+let charts = {};
+function renderChart(id, type, labels, datasets) {
+  if (charts[id]) charts[id].destroy();
+  const canvas = document.getElementById(id);
+  if (!canvas) return;
+  charts[id] = new Chart(canvas.getContext('2d'), {
+    type,
+    data: { labels, datasets },
+    options: { responsive: true, maintainAspectRatio: false }
+  });
 }
 
 async function loadStats() {
@@ -472,11 +530,151 @@ async function loadStats() {
         ${data.weakest_section ? ` · Eng past ko'rsatkich: <span class="badge warning">${sectionNames[data.weakest_section] || data.weakest_section}</span>` : ''}
       </div>
     `;
+    const daily = data.daily_activity || [];
+    renderChart('activityChart', 'line', daily.map(d => d.date), [{
+      label: "Faol talabalar (kunlik)", data: daily.map(d => d.count),
+      borderColor: '#1a56e8', backgroundColor: 'rgba(26,86,232,0.1)', fill: true, tension: 0.1
+    }]);
   } catch (e) {
     body.innerHTML = `<div class="empty-state">→ ${e.message}</div>`;
   }
 }
 
+// ─── WRITING GRADING ──────────────────────────────────────
+let writingFilter = 'pending';
+let allWriting = [];
+
+function setWritingFilter(f) {
+  writingFilter = f;
+  document.querySelectorAll('.filter-btn').forEach(b => b.classList.toggle('active', b.dataset.wfilter === f));
+  loadWriting();
+}
+
+async function loadWriting() {
+  const listEl = document.getElementById('writing-list');
+  const emptyEl = document.getElementById('writing-empty');
+  listEl.innerHTML = `<div class="empty-state"><span class="spinner"></span> Yuklanmoqda...</div>`;
+  emptyEl.style.display = 'none';
+  try {
+    allWriting = await apiCall(T_API, `/writing?status=${writingFilter}`);
+    renderWriting();
+  } catch (e) {
+    listEl.innerHTML = '';
+    emptyEl.textContent = '→ ' + e.message;
+    emptyEl.style.display = 'block';
+  }
+}
+
+function renderWriting() {
+  const listEl = document.getElementById('writing-list');
+  const emptyEl = document.getElementById('writing-empty');
+
+  if (!allWriting.length) {
+    listEl.innerHTML = '';
+    emptyEl.textContent = writingFilter === 'pending' ? "Baholanadigan Writing topshiriqlari yo'q" : "Bu bo'limda natijalar yo'q";
+    emptyEl.style.display = 'block';
+    return;
+  }
+  emptyEl.style.display = 'none';
+
+  listEl.innerHTML = allWriting.map(r => {
+    const date = new Date(r.submitted_at).toLocaleString('uz-UZ', {day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});
+    const gradedBadge = r.band !== null
+      ? `<span class="badge success">Band ${r.band}</span>`
+      : `<span class="badge warning">Baholanmagan</span>`;
+
+    let gradeForm = '';
+    if (r.band === null) {
+      gradeForm = `
+      <div class="grade-form">
+        <div>
+          <label>Band (0-9)</label>
+          <input type="number" step="0.5" min="0" max="9" id="wband-${r.id}" placeholder="6.5">
+        </div>
+        <div style="flex:2;">
+          <label>Izoh (ixtiyoriy)</label>
+          <textarea id="wfeedback-${r.id}" placeholder="Grammatika yaxshi, lekin so'z boyligini oshirish kerak..."></textarea>
+        </div>
+        <button class="btn-primary btn-sm" onclick="gradeWriting(${r.id}, ${r.student_id})" id="wgrade-btn-${r.id}">✓ Saqlash va yuborish</button>
+      </div>`;
+    } else if (r.writing_feedback) {
+      gradeForm = `<div style="padding:0 20px 16px; font-size:13px; color:var(--muted);"><b>Izoh:</b> ${r.writing_feedback}</div>`;
+    }
+
+    return `
+    <div class="result-card">
+      <div class="result-head">
+        <div>
+          <div class="result-name">${r.full_name}</div>
+          <div class="result-email">${r.email}</div>
+        </div>
+        <div style="display:flex; align-items:center; gap:8px;">
+          ${gradedBadge}
+          <span class="badge muted">${date}</span>
+        </div>
+      </div>
+      <div class="result-body">
+        ${r.writing_task1 ? `<div class="essay-label">Task 1</div><div class="essay-box">${escapeHtml(r.writing_task1)}</div>` : ''}
+        ${r.writing_task2 ? `<div class="essay-label">Task 2</div><div class="essay-box">${escapeHtml(r.writing_task2)}</div>` : ''}
+      </div>
+      ${gradeForm}
+    </div>`;
+  }).join('');
+}
+
+function escapeHtml(text) {
+  const div = document.createElement('div');
+  div.textContent = text || '';
+  return div.innerHTML;
+}
+
+async function gradeWriting(resultId, studentId) {
+  const band = document.getElementById(`wband-${resultId}`).value;
+  const feedback = document.getElementById(`wfeedback-${resultId}`).value.trim();
+  if (!band) { alert('Band score kiriting'); return; }
+
+  const btn = document.getElementById(`wgrade-btn-${resultId}`);
+  btn.disabled = true;
+  btn.innerHTML = '<span class="spinner"></span> Yuborilmoqda...';
+
+  try {
+    await apiCall(T_API, `/students/${studentId}/grade-writing`, {
+      method: 'POST',
+      headers: {'Content-Type': 'application/json'},
+      body: JSON.stringify({ result_id: resultId, band: parseFloat(band), feedback, send_email: true })
+    });
+    await loadWriting();
+  } catch (e) {
+    alert('Xatolik: ' + e.message);
+    btn.disabled = false;
+    btn.innerHTML = '✓ Saqlash va yuborish';
+  }
+}
+
+// ─── EXPORT ───────────────────────────────────────────────
+async function exportResults(format) {
+  try {
+    const res = await fetch(`${T_API}/export/results?format=${format}`, {
+      headers: token ? { 'Authorization': `Bearer ${token}` } : {}
+    });
+    if (!res.ok) {
+      const data = await res.json().catch(() => ({}));
+      throw new Error(data.detail || 'Export xatosi');
+    }
+    const blob = await res.blob();
+    const url = URL.createObjectURL(blob);
+    const a = document.createElement('a');
+    a.href = url;
+    a.download = `guruh_natijalar.${format === 'excel' ? 'xlsx' : 'csv'}`;
+    document.body.appendChild(a);
+    a.click();
+    a.remove();
+    URL.revokeObjectURL(url);
+  } catch (e) {
+    alert('Xatolik: ' + e.message);
+  }
+}
+
 // ─── INIT ─────────────────────────────────────────────────
 (async function init() {
   if (!token) { showView('view-auth'); return; }
diff --git a/teacher_routes.py b/teacher_routes.py
index 7356fde..88f2e75 100644
--- a/teacher_routes.py
+++ b/teacher_routes.py
@@ -1,12 +1,24 @@
+from typing import Optional
+
 from fastapi import APIRouter, Depends, HTTPException
+from pydantic import BaseModel
 
 from db import get_pool
 from auth import get_current_teacher
 from scoring import get_band_score, calculate_overall_band
+from grading import apply_writing_grade
+from exports import export_rows
 
 router = APIRouter(prefix="/api/teacher", tags=["teacher"])
 
 
+class TeacherGradeWritingIn(BaseModel):
+    result_id: int
+    band: float
+    feedback: Optional[str] = None
+    send_email: bool = True
+
+
 async def _own_active_group_or_error(conn, teacher_id: int):
     group = await conn.fetchrow(
         "SELECT id, name, invite_code, is_active, created_at FROM groups WHERE teacher_id=$1",
@@ -130,6 +142,18 @@ async def teacher_stats(current_user: dict = Depends(get_current_teacher)):
             group["id"]
         )
 
+        daily_rows = await conn.fetch(
+            """
+            SELECT DATE(er.submitted_at) AS date, COUNT(DISTINCT er.email) AS count
+            FROM exam_results er
+            JOIN users u ON u.email = er.email
+            WHERE u.group_id = $1 AND er.submitted_at >= NOW() - INTERVAL '30 days'
+            GROUP BY DATE(er.submitted_at)
+            ORDER BY date
+            """,
+            group["id"]
+        )
+
     bands_by_section = {}
     for r in rows:
         band = _band_for(r)
@@ -143,4 +167,113 @@ async def teacher_stats(current_user: dict = Depends(get_current_teacher)):
         "average_band_by_section": averages,
         "weakest_section": weakest_section,
         "total_attempts": len(rows),
+        "daily_activity": [{"date": str(r["date"]), "count": r["count"]} for r in daily_rows],
     }
+
+
+# ─── Writing baholash ───────────────────────────────────────────────────────
+
+@router.get("/writing")
+async def list_group_writing(status: str = "pending", current_user: dict = Depends(get_current_teacher)):
+    """Guruhdagi talabalarning Writing topshiriqlari ro'yxati — insho matni bilan birga,
+    shunda teacher baho qo'yishdan oldin matnni o'qiy oladi. status: pending | graded | all"""
+    if status not in ("pending", "graded", "all"):
+        raise HTTPException(status_code=400, detail="status noto'g'ri (pending, graded yoki all bo'lishi kerak)")
+
+    db = await get_pool()
+    async with db.acquire() as conn:
+        group = await _own_active_group_or_error(conn, current_user["id"])
+
+        query = """
+            SELECT er.id, er.full_name, er.email, u.id AS student_id,
+                   er.writing_task1, er.writing_task2,
+                   er.writing_band, er.writing_feedback, er.notified, er.submitted_at
+            FROM exam_results er
+            JOIN users u ON u.email = er.email
+            WHERE u.group_id = $1 AND er.section = 'writing'
+        """
+        if status == "pending":
+            query += " AND er.writing_band IS NULL"
+        elif status == "graded":
+            query += " AND er.writing_band IS NOT NULL"
+        query += " ORDER BY er.submitted_at DESC LIMIT 200"
+
+        rows = await conn.fetch(query, group["id"])
+
+    return [
+        {
+            "id": r["id"], "student_id": r["student_id"],
+            "full_name": r["full_name"], "email": r["email"],
+            "writing_task1": r["writing_task1"], "writing_task2": r["writing_task2"],
+            "band": float(r["writing_band"]) if r["writing_band"] is not None else None,
+            "writing_feedback": r["writing_feedback"],
+            "notified": r["notified"],
+            "submitted_at": r["submitted_at"].isoformat(),
+        }
+        for r in rows
+    ]
+
+
+@router.post("/students/{student_id}/grade-writing")
+async def teacher_grade_writing(
+    student_id: int, data: TeacherGradeWritingIn, current_user: dict = Depends(get_current_teacher)
+):
+    """Teacher o'z guruhidagi talabaning Writing natijasini baholaydi. MUHIM: natija
+    (result_id) chindan ham shu talabaga va 'writing' bo'limiga tegishli ekani
+    tekshiriladi — aks holda teacher boshqa guruh natijasini ID orqali baholab
+    qo'yishi mumkin edi (IDOR)."""
+    db = await get_pool()
+    async with db.acquire() as conn:
+        group = await _own_active_group_or_error(conn, current_user["id"])
+        student = await conn.fetchrow(
+            "SELECT id, email FROM users WHERE id=$1 AND group_id=$2",
+            student_id, group["id"]
+        )
+        if not student:
+            raise HTTPException(status_code=404, detail="Talaba topilmadi")
+
+        result = await conn.fetchrow(
+            "SELECT id FROM exam_results WHERE id=$1 AND email=$2 AND section='writing'",
+            data.result_id, student["email"]
+        )
+        if not result:
+            raise HTTPException(status_code=404, detail="Writing natijasi topilmadi")
+
+    return await apply_writing_grade(data.result_id, data.band, data.feedback, data.send_email)
+
+
+# ─── Export ──────────────────────────────────────────────────────────────────
+
+@router.get("/export/results")
+async def export_group_results(format: str = "csv", current_user: dict = Depends(get_current_teacher)):
+    """Guruh natijalarini Excel/CSV qilib yuklab olish — ota-onalarga yoki markaz
+    hisobotiga tayyor holda beriladi."""
+    db = await get_pool()
+    async with db.acquire() as conn:
+        group = await _own_active_group_or_error(conn, current_user["id"])
+        rows = await conn.fetch(
+            """
+            SELECT er.id, er.full_name, er.email, er.section, er.score, er.total,
+                   er.writing_band, er.submitted_at
+            FROM exam_results er
+            JOIN users u ON u.email = er.email
+            WHERE u.group_id = $1
+            ORDER BY er.submitted_at DESC
+            """,
+            group["id"]
+        )
+
+    data = []
+    for r in rows:
+        band = _band_for(r)
+        data.append({
+            "ID": r["id"],
+            "Ism": r["full_name"],
+            "Email": r["email"],
+            "Bo'lim": r["section"],
+            "Natija": f"{r['score']}/{r['total']}" if r["score"] is not None else "",
+            "Band": band if band is not None else "",
+            "Sana": r["submitted_at"].isoformat(),
+        })
+
+    return export_rows(data, "guruh_natijalar", format, "Natijalar")
